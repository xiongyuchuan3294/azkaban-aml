# 需求样例 01：新增平均交易额风险规则
## 需求描述
1. 在 DWS 账户风险聚合表中新增指标 `avg_amt`（日均交易额）。
2. 在 ADS 预警生成逻辑中新增规则 `R004`：
   - 规则含义：账户当日交易笔数较多且平均交易额较高，判定为高风险。
   - 命中条件：`txn_cnt >= 3` 且 `avg_amt >= 60000`。
   - 风险等级：`HIGH`。
   - 证据字段：输出 `txn_cnt` 与 `avg_amt`。
